import React from "react";

const SubHeading = ({ children }: { children?: React.ReactNode }) => {
  return <div className="font-jakarta text-[#525252] text-base leading-snug text-center font-normal">{children}</div>;
};

export default SubHeading;
